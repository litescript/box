# ~/src/box/box
#!/bin/bash
set -euo pipefail
export LC_ALL=C

SELF="$(readlink -f "${BASH_SOURCE[0]}")"
DIR="$(cd "$(dirname "$SELF")" && pwd)"

# shellcheck disable=SC1091
. "$DIR/box-env.sh"
# shellcheck disable=SC1091
. "$DIR/box-util.sh"
# shellcheck disable=SC1091
. "$DIR/box-recipe.sh"
# shellcheck disable=SC1091
. "$DIR/box-fs.sh"
# shellcheck disable=SC1091
. "$DIR/box-adopt.sh"
# shellcheck disable=SC1091
. "$DIR/box-cmds.sh"
# shellcheck disable=SC1091
. "$DIR/box-index.sh"

main "$@"
